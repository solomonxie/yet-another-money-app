# Shared Components

- `ui/` — every reusable component (buttons, modals, pickers, form fields); namespaced here rather than flattened so imports read as `components/ui/Foo`, leaving room for a non-UI shared-component category later without a rename
