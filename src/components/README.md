# Shared Components

Reusable UI building blocks used across screens — buttons, modals, pickers, form fields. Namespaced under `ui/` rather than flattened here, so imports read as `components/ui/Foo` and there's room for a non-UI shared-component category later without a rename.
